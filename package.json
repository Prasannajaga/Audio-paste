{
  "name": "temp_scaffold",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build": "node scripts/prepare-tauri-config.mjs && tauri build --config src-tauri/tauri.generated.conf.json",
    "tauri:dev": "node scripts/prepare-tauri-config.mjs && tauri dev --config src-tauri/tauri.generated.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
